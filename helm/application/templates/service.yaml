apiVersion: v1
kind: Service
metadata:
  name: application
spec:
  type: {{ .Values.service.type }}
  selector:
    app: application
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
